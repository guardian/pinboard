# Find full documentation here https://docs.github.com/en/actions/learn-github-actions/workflow-syntax-for-github-actions
name: CI

on:
  # Manual invocation.
  workflow_dispatch:

  push:

jobs:
  CI:
    runs-on: ubuntu-latest

    # See https://docs.github.com/en/actions/security-guides/automatic-token-authentication#permissions-for-the-github_token
    permissions:
      # required by aws-actions/configure-aws-credentials
      id-token: write
      contents: read
      pull-requests: write
    steps:
      - name: calculate build number
        run: |
          LAST_TEAMCITY_BUILD=650
          echo "BUILD_NUMBER=$(( $GITHUB_RUN_NUMBER + $LAST_TEAMCITY_BUILD ))" >> $GITHUB_ENV

      - uses: actions/checkout@v4

      # Setup AWS credentials to enable uploading to S3 for Riff-Raff.
      # See https://github.com/aws-actions/configure-aws-credentials
      - uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.GU_RIFF_RAFF_ROLE_ARN }}
          aws-region: eu-west-1

      - run: corepack enable

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version-file: ".nvmrc"

      - name: install dependencies
        run: yarn install --frozen-lockfile

      - name: check for duplicate dependencies (if it fails here, then pre-commit hook is not working)
        run: yarn dedupe --check

      - name: generate TS definitions for GraphQL schema
        run: yarn graphql-refresh

      - name: perform a prettier check
        run: yarn prettier-check

      - name: perform a linting check
        run: yarn lint

      - name: write the current GIT hash to GIT_COMMIT_HASH.ts (so it's available at build time - so it gets baked into the artifact)
        run: |
          GIT_COMMIT_HASH=$(git rev-parse HEAD)
          echo 'export const GIT_COMMIT_HASH = "'$GIT_COMMIT_HASH'";' > GIT_COMMIT_HASH.ts

      - name: write the current BUILD_NUMBER to BUILD_NUMBER.ts (so it's available at build time - so it gets baked into the artifact)
        run: echo 'export const BUILD_NUMBER = "'$BUILD_NUMBER'";' > BUILD_NUMBER.ts

      - name: test all workspaces (i.e. modules)
        run: yarn test

      - name: build all workspaces (i.e. modules)
        run: yarn build

      - name: combine client and bootstrapping-lambda into directory
        run: |
          mkdir -p dist/client
          cp -v bootstrapping-lambda/dist/index.js dist/
          cp -v client/dist/pinboard.main* dist/client
          cp -rv client/dist/push-notifications dist/client
          zip -r dist/pinboard-bootstrapping-lambda-api.zip dist/*

      - name: zip subprojects
        run: |
          zip -r -j workflow-bridge-lambda/pinboard-workflow-bridge-lambda.zip workflow-bridge-lambda/dist/
          zip -r -j grid-bridge-lambda/pinboard-grid-bridge-lambda.zip grid-bridge-lambda/dist/
          zip -r -j users-refresher-lambda/pinboard-users-refresher-lambda.zip users-refresher-lambda/dist/
          zip -r -j notifications-lambda/pinboard-notifications-lambda.zip notifications-lambda/dist/
          zip -r -j auth-lambda/pinboard-auth-lambda.zip auth-lambda/dist/
          zip -r -j database-bridge-lambda/pinboard-database-bridge-lambda.zip database-bridge-lambda/dist/
          zip -r -j archiver-lambda/pinboard-archiver-lambda.zip archiver-lambda/dist/
          zip -r -j email-lambda/pinboard-email-lambda.zip email-lambda/dist/

      - uses: guardian/actions-riff-raff@v4
        with:
          projectName: Editorial Tools::PinBoard
          configPath: riff-raff.yaml
          contentDirectories: |
            pinboard-cloudformation:
              - cdk/cdk.out/PinBoardStack-CODE.template.json
              - cdk/cdk.out/PinBoardStack-PROD.template.json
            pinboard-db-jump-host-asg-artifact:
              - cdk/startup.sh
            pinboard-bootstrapping-lambda-api:
              - dist/pinboard-bootstrapping-lambda-api.zip
            pinboard-workflow-bridge-lambda:
              - workflow-bridge-lambda/pinboard-workflow-bridge-lambda.zip 
            pinboard-grid-bridge-lambda:
              - grid-bridge-lambda/pinboard-grid-bridge-lambda.zip
            pinboard-users-refresher-lambda:
              - users-refresher-lambda/pinboard-users-refresher-lambda.zip
            pinboard-notifications-lambda:
              - notifications-lambda/pinboard-notifications-lambda.zip
            pinboard-auth-lambda:
              - auth-lambda/pinboard-auth-lambda.zip
            pinboard-database-bridge-lambda:
              - database-bridge-lambda/pinboard-database-bridge-lambda.zip
            pinboard-archiver-lambda:
              - archiver-lambda/pinboard-archiver-lambda.zip
            pinboard-email-lambda:
              - email-lambda/pinboard-email-lambda.zip
          githubToken: ${{ secrets.GITHUB_TOKEN }}
          roleArn: ${{ secrets.GU_RIFF_RAFF_ROLE_ARN }}
          buildNumber: ${{ env.BUILD_NUMBER }}
