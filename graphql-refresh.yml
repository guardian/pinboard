overwrite: true
schema:
  - "shared/graphql/schema.graphql"
  - "shared/graphql/aws.graphql"
documents: null
generates:
  shared/graphql/graphql.ts:
    plugins:
      - "typescript"
    config:
      avoidOptionals: true
config:
  scalars:
    AWSJSON: string
    AWSTimestamp: number
    AWSDateTime: string
