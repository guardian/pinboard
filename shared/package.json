{
  "name": "shared",
  "version": "1.0.0",
  "description": "Shared code between the other modules",
  "scripts": {
    "type-check": "tsc --noEmit",
    "build": "tsc --noEmit"
  },
  "devDependencies": {
    "@aws-sdk/client-s3": "^3.299.0",
    "@aws-sdk/client-ssm": "^3.299.0",
    "@aws-sdk/credential-providers": "^3.299.0",
    "@aws-sdk/rds-signer": "^3.299.0"
  },
  "dependencies": {
    "postgres": "^3.4.5"
  }
}
